type Usuario {
  id: ID
  nome: String!
  email: String!
  idade: Int
  perfil: Perfil
  status:UsuarioStatus
}

enum UsuarioStatus{
  ATIVO
  INATIVO
  BLOQUEADO
}

type Perfil {
  id: ID
  nome: String
}
